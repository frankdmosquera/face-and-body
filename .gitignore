# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright mcp session exhaust
.playwright-mcp/

# staging folder for ImageKit uploads; photos live in ImageKit, never in the repo
blueprint/reference/upload/

# Eminence product research, local only and deliberately not pushed.
# The scraped catalogue and the product shots downloaded from their site,
# both still raw: descriptions are Eminence's own words, and the shipping
# assets should come from her Spa Partner portal rather than from here.
# Un-ignore whichever parts survive the rewrite; until then nothing about
# this belongs on GitHub.
/data/products/
/public/images/
